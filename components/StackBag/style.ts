import { Colors } from "@/constants/Colors";
import { StyleSheet } from "react-native";

export const StackBagStyle = StyleSheet.create({
  container: {
    display: "flex",
    flexDirection: "row",
    gap: 16,
    paddingVertical: 10,
    position: "relative",
  },
});
